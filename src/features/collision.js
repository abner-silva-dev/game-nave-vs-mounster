// Conllision

// if (
//   enviroment.enemy?.enemies.some((enemy, i) => {
//     if (board?.at(enemy.position.y + 1)?.at(enemy.position.x) === 1) {
//       indexEnemies.push(i);
//       return true;
//     }
//     return false;
//   })
// ) {
//   indexEnemies.forEach((indexEnemy) =>
//     enviroment.enemy.enemies.splice(indexEnemy, 1)
//   );
//   renderLives(--NUMBER_OF_LIVES);
//   // if (!wasBuller) {
//   // }
//   // wasBuller = true;
// }
